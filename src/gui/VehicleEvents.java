package gui;

/**
 * An enum of all the events than can be broadcast by a car observable
 */
public enum VehicleEvents {
    GAS,
    BREAK,
    START_ALL,
    STOP_ALL,
    TURBO_ON,
    TURBO_OFF,
    RAISE_BOARD,
    LOWER_BED,
    ADD,
    REMOVE
}